<UserControl x:Class="MyWpfApp.LoginMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyWpfApp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:Visibility="Visible"
             Unloaded="UserControl_Unloaded" Visibility="Collapsed">

    <Border Background="{DynamicResource MainBackground}">
        <Grid>
            <Border Margin="20" Background="White" BorderBrush="Gray" CornerRadius="10" Width="250" Height="200">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="0" Opacity="0.7"/>
                </Border.Effect>

                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <ContentControl x:Name="ActiveItem" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </ScrollViewer>
            </Border> 
        </Grid>
    </Border>
</UserControl>
